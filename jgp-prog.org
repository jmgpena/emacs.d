* Smartparens

#+BEGIN_SRC emacs-lisp
  (add-to-list 'el-get-sources
               '(:name smartparens
                       :after (progn
                                (require 'smartparens-config)
                                (smartparens-global-mode t))))
#+END_SRC

* PHP

#+BEGIN_SRC emacs-lisp
  (add-to-list 'el-get-sources
               '(:name php-mode))
#+END_SRC
